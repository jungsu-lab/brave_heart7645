# jungsu blog 관리자 사용법

관리 화면은 배포된 사이트의 `/admin/`에서 연다.

## 처음 한 번 로그인하기

1. GitHub의 **Fine-grained personal access token** 생성 화면을 연다.
2. Repository access에서 `jungsu-lab/brave_heart7645` 저장소만 선택한다.
3. Repository permissions에서 **Contents: Read and write** 권한만 추가한다.
4. 생성한 토큰을 `/admin/` 로그인 화면에 붙여 넣는다.

토큰은 사이트 코드나 Vercel 환경 변수에 저장되지 않고 로그인한 브라우저의 로컬 저장소에만 남는다. 공용 컴퓨터에서는 사용하지 않고, 노출됐다고 생각되면 GitHub에서 즉시 토큰을 폐기한다.

## 보안 수칙

- GitHub 계정에는 2단계 인증(2FA)을 켠다.
- 토큰은 `brave_heart7645` 저장소 한 곳에만 접근하도록 제한하고 만료일을 설정한다.
- 공용 PC나 공용 브라우저에서는 관리자 화면에 로그인하지 않는다.
- 토큰이나 비밀번호를 글 본문, 설정 파일, 커밋에 적지 않는다.
- 노출이 의심되는 토큰은 파일에서 지우는 것만으로 끝내지 않고 GitHub에서 즉시 폐기한 뒤 새로 만든다.

관리자 페이지에는 CSP, 클릭재킹 방지, 브라우저 권한 제한 헤더가 적용된다. CMS 스크립트는 검증한 버전과 무결성 해시로 고정되어 있으므로, 버전을 올릴 때는 스크립트 주소와 `integrity` 값을 함께 갱신해야 한다.

## 글 작성과 수정

- **데일리와 공부 글**: Daily와 Study에 표시되는 글
- **프로젝트**: Projects에 표시되는 프로젝트 기록
- **AI 영상**: AI Videos에 표시되는 영상 기록

새 글의 파일명과 URL은 제목에서 자동으로 만들어진다. 제목을 나중에 바꿔도 기존 글의 URL은 유지된다.

저장 버튼을 누르면 `main` 브랜치에 Markdown 파일이 커밋된다. Vercel 배포가 끝난 뒤 공개 사이트에 반영된다.

이미지는 편집기에서 업로드할 수 있으며 `public/images/uploads`에 저장된다.
